'use client';

import MountingInfoSection, { type MountingInfoSectionProps } from '@/app/features/stringing-applications/components/apply-shared/MountingInfoSection';

export default function Step2MountingInfo(props: MountingInfoSectionProps) {
  return <MountingInfoSection {...props} />;
}

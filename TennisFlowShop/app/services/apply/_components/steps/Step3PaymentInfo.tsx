'use client';

import PaymentInfoSection, { type PaymentInfoSectionProps } from '@/app/features/stringing-applications/components/apply-shared/PaymentInfoSection';

export default function Step3PaymentInfo(props: PaymentInfoSectionProps) {
  return <PaymentInfoSection {...props} />;
}

'use client';

import FinalRequestSection, { type FinalRequestSectionProps } from '@/app/features/stringing-applications/components/apply-shared/FinalRequestSection';

export default function Step4FinalRequest(props: FinalRequestSectionProps) {
  return <FinalRequestSection {...props} />;
}

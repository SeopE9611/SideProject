"use client";

import type { CareItem, StartCreateOptions } from "@/app/mypage/racket-care/_components/racket-care-client.types";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Plus, Target } from "lucide-react";
import Link from "next/link";

const freqLabels: Record<string, string> = { monthly: "월 1~2회", weekly: "주 1회", biweekly_plus: "주 2~3회", heavy: "주 4회 이상" };
const stateMeta = { good: { label: "양호", badge: "success" }, prepare: { label: "교체 준비", badge: "warning" }, due: { label: "교체 권장", badge: "danger" } } as const;
function dateLabel(value: string) { return new Intl.DateTimeFormat("ko-KR", { year: "numeric", month: "short", day: "numeric" }).format(new Date(value)); }
function dday(days: number) { if (days > 0) return `D-${days}`; if (days === 0) return "D-DAY"; return `${Math.abs(days)}일 지남`; }
function recommendHref(item: CareItem) { return `/products/recommend?from=racket-care&careItemId=${item.id}&freq=${item.playFrequency}`; }
function scrollToSection(id: string) { const target = document.getElementById(id); const reduced = window.matchMedia("(prefers-reduced-motion: reduce)").matches; target?.scrollIntoView({ behavior: reduced ? "auto" : "smooth", block: "start" }); }

export default function RacketCareHero({ selected, onCreate }: { selected: CareItem | null; onCreate: (options?: StartCreateOptions) => void }) {
  const meta = selected ? stateMeta[selected.careStatus.state] : null;
  return <section className="relative overflow-hidden rounded-hero border border-border bg-card p-5 shadow-soft bp-sm:p-8 bp-lg:p-12"><div className="pointer-events-none absolute right-8 top-8 hidden h-56 w-56 rounded-full bg-brand-highlight-muted bp-lg:block" /><div className="relative grid gap-9 bp-lg:grid-cols-[minmax(0,1.08fr)_minmax(380px,0.92fr)] bp-lg:items-center"><div className="space-y-6"><div className="flex flex-wrap items-center gap-2"><Badge variant="signal">NEW · RACKET CARE PASS</Badge><span className="text-ui-label text-muted-foreground">내 라켓 전용 관리 대시보드</span></div><h1 className="break-keep font-brand-display text-[2.75rem] font-semibold leading-none tracking-tight text-foreground bp-sm:text-[4.4rem] bp-lg:text-[5.2rem]">내 라켓의<br />다음 교체일,<br /><span className="text-brand-outline">먼저 알려드려요.</span></h1><p className="max-w-xl break-keep text-ui-body text-muted-foreground">마지막 교체일과 플레이 빈도만 연결하면 스트링 상태, 교체 시점, 맞춤 추천까지 한 화면에서 이어집니다.</p><div className="grid gap-2 bp-sm:flex bp-sm:flex-wrap">{selected ? <Button asChild variant="highlight" wrap="responsive"><Link href={recommendHref(selected)}>맞춤 스트링 추천받기</Link></Button> : <Button variant="highlight" wrap="responsive" onClick={() => onCreate()}><Plus className="h-4 w-4" />내 라켓 등록하기</Button>}<Button variant="outline" wrap="responsive" onClick={() => scrollToSection(selected ? "racket-care-workspace" : "racket-care-flow")}>{selected ? "내 라켓 관리" : "사용 흐름 보기"}</Button></div><div className="grid max-w-lg grid-cols-3 divide-x divide-border border-y border-border py-3"><div className="pr-3"><p className="font-brand-display text-ui-card-title">30초</p><p className="text-ui-label text-muted-foreground">간편 등록</p></div><div className="px-3"><p className="font-brand-display text-ui-card-title">1-TAP</p><p className="text-ui-label text-muted-foreground">이력 활용</p></div><div className="pl-3"><p className="font-brand-display text-ui-card-title">무료</p><p className="text-ui-label text-muted-foreground">교체 알림</p></div></div></div><div className="relative"><div className="pointer-events-none absolute inset-6 rounded-full bg-brand-highlight-muted" />{selected ? <><Card variant="feature" className="relative mx-auto max-w-md rounded-panel shadow-float"><CardContent className="p-5 bp-sm:p-6"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="text-ui-kicker text-muted-foreground">MY RACKET</p><h2 className="mt-2 break-words font-brand-display text-ui-card-title-lg">{selected.nickname}</h2><p className="break-words text-ui-label text-muted-foreground">{selected.racket.brand} {selected.racket.model}</p></div>{meta ? <Badge variant={meta.badge} wrap="normal">{meta.label}</Badge> : null}</div><div className="mx-auto mt-6 grid w-full max-w-[230px] place-items-center"><div role="progressbar" aria-label={`스트링 상태 점수 ${selected.careStatus.lifeScore}점`} aria-valuemin={0} aria-valuemax={100} aria-valuenow={selected.careStatus.lifeScore} className="grid aspect-square w-full place-items-center rounded-full border border-border shadow-soft" style={{ background: `conic-gradient(hsl(var(--brand-highlight)) ${selected.careStatus.lifeScore}%, hsl(var(--muted)) 0)` }}><div className="grid h-[72%] w-[72%] place-items-center rounded-full bg-card text-center"><span className="font-brand-display text-ui-display">{selected.careStatus.lifeScore}</span><span className="text-ui-label text-muted-foreground">STRING LIFE</span></div></div></div><p className="mt-6 text-center text-ui-body-sm text-muted-foreground">예상 교체일 <span className="font-semibold text-foreground">{dateLabel(selected.careStatus.nextRecommendedAt)}</span></p></CardContent></Card><div className="relative mt-3 grid gap-3 bp-sm:grid-cols-3"><Card variant="floating"><CardContent className="p-3"><p className="text-ui-label text-muted-foreground">D-day</p><p className="font-semibold">{dday(selected.careStatus.daysRemaining)}</p></CardContent></Card><Card variant="floating"><CardContent className="p-3"><p className="text-ui-label text-muted-foreground">플레이 빈도</p><p className="font-semibold">{freqLabels[selected.playFrequency] ?? selected.playFrequency}</p></CardContent></Card><Card variant="floating"><CardContent className="p-3"><p className="text-ui-label text-muted-foreground">최근 스트링</p><p className="break-words font-semibold">{selected.stringSnapshot?.name || "기록 없음"}</p></CardContent></Card></div></> : <Card variant="feature" className="relative mx-auto max-w-md rounded-panel shadow-float"><CardContent className="grid gap-5 p-6 text-center bp-sm:p-8"><div className="mx-auto grid h-24 w-24 place-items-center rounded-full bg-brand-highlight-muted text-brand-highlight-foreground dark:text-brand-highlight"><Target className="h-10 w-10" /></div><div><p className="text-ui-kicker text-muted-foreground">PREVIEW DASHBOARD</p><h2 className="mt-2 break-keep font-brand-display text-ui-card-title-lg">라켓을 등록하면 실제 상태 점수가 표시됩니다.</h2></div><p className="break-keep text-ui-body-sm text-muted-foreground">프로필과 완료된 교체 이력을 가져와 더 빠르게 시작할 수 있어요.</p><Button variant="highlight" wrap="responsive" onClick={() => onCreate()}>내 라켓 등록하기</Button></CardContent></Card>}</div></div></section>;
}

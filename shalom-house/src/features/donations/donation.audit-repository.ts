import {ObjectId,type ClientSession,type Db}from"mongodb";import{donationAuditActions,donationAuditFields,type DonationAuditDocument}from"./donation.audit";import{isCanonicalDateOnly,isDonationMethod,isDonationPurpose,isDonationReceiptStatus,isDonationStatus,isValidDonationDate}from"./donation.types";
const snapshot=(v:any)=>v&&typeof v.anonymous==="boolean"&&(v.donorReference===null||typeof v.donorReference==="string")&&isCanonicalDateOnly(v.donatedOn)&&Number.isInteger(v.amountWon)&&isDonationMethod(v.method)&&isDonationPurpose(v.purpose)&&typeof v.purposeDescription==="string"&&isDonationReceiptStatus(v.receiptStatus)&&(v.receiptIssuedOn===null||isCanonicalDateOnly(v.receiptIssuedOn))&&isDonationStatus(v.status);
export async function insertDonationAuditEvent(db:Db,document:DonationAuditDocument,session:ClientSession){await db.collection<DonationAuditDocument>("donation_audit_events").insertOne(document,{session});}export async function listDonationAuditEvents(db:Db,donationId:ObjectId){const rows=await db.collection("donation_audit_events").find({donationId},{projection:{action:1,"actor.displayName":1,occurredAt:1,changedFields:1,before:1,after:1}}).sort({occurredAt:-1,_id:-1}).limit(50).toArray();return rows.filter(v=>donationAuditActions.includes(v.action)&&typeof v.actor?.displayName==="string"&&isValidDonationDate(v.occurredAt)&&Array.isArray(v.changedFields)&&v.changedFields.every((x:unknown)=>donationAuditFields.includes(x as never))&&(v.before===null||snapshot(v.before))&&snapshot(v.after)).map(v=>({action:v.action,displayName:v.actor.displayName,occurredAt:v.occurredAt.toISOString(),changedFields:v.changedFields,fromStatus:v.before?.status??null,toStatus:v.after.status}));}

import{ObjectId,type ClientSession,type Db}from"mongodb";import{isAdminRole,adminUserStatuses,type AdminUserStatus}from"@/features/admin-auth/admin-auth.types";import{validEmail,validName}from"./admin-user.validation";import{adminUserAuditActions,adminUserAuditChangedFields,type MongoAdminUserAuditEvent}from"./admin-user.audit";
export const ADMIN_USER_AUDIT_COLLECTION_NAME="admin_user_audit_events";
function validDate(v:unknown):v is Date{return v instanceof Date&&!Number.isNaN(v.getTime())}function snapshot(v:any){return v&&validEmail(v.email)&&validName(v.displayName)&&isAdminRole(v.role)&&adminUserStatuses.includes(v.status as AdminUserStatus)}
export function isValidAdminUserAuditEvent(v:any):v is MongoAdminUserAuditEvent{return v&&v._id instanceof ObjectId&&v.adminUserId instanceof ObjectId&&v.schemaVersion===1&&adminUserAuditActions.includes(v.action)&&v.actor?.adminId instanceof ObjectId&&validName(v.actor.displayName)&&isAdminRole(v.actor.role)&&validDate(v.occurredAt)&&(v.fromVersionAt===null||validDate(v.fromVersionAt))&&validDate(v.toVersionAt)&&v.occurredAt.getTime()===v.toVersionAt.getTime()&&(v.before===null||snapshot(v.before))&&snapshot(v.after)&&Array.isArray(v.changedFields)&&new Set(v.changedFields).size===v.changedFields.length&&v.changedFields.every((x:any)=>adminUserAuditChangedFields.includes(x))}
export async function insertAdminUserAuditEvent(db:Db,event:MongoAdminUserAuditEvent,session:ClientSession){if(!isValidAdminUserAuditEvent(event))throw new Error("InvalidAdminUserAuditEvent");await db.collection<MongoAdminUserAuditEvent>(ADMIN_USER_AUDIT_COLLECTION_NAME).insertOne(event,{session})}

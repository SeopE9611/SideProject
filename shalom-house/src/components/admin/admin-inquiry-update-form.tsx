"use client";
import { useState, type FormEvent } from "react";
import { inquiryStatusLabels, type InquiryStatus } from "@/features/inquiries/inquiry.types";
export function AdminInquiryUpdateForm({ id, expectedUpdatedAt, initialStatus, initialInternalNote }: { id: string; expectedUpdatedAt: string; initialStatus: InquiryStatus; initialInternalNote: string }) { const transitions: Record<InquiryStatus, InquiryStatus[]> = { received: ["received", "in_review", "completed"], in_review: ["in_review", "contacted", "completed"], contacted: ["contacted", "in_review", "completed"], completed: ["completed", "in_review", "archived"], archived: ["archived", "in_review"] }; const options = transitions[initialStatus]; const [status, setStatus] = useState(initialStatus); const [note, setNote] = useState(initialInternalNote); const [confirmed, setConfirmed] = useState(false); const [busy, setBusy] = useState(false); const [message, setMessage] = useState(""); async function submit(e: FormEvent) { e.preventDefault(); if (!confirmed || busy) return; setBusy(true); try { const response = await fetch(`/api/admin/inquiries/${id}`, { method: "PUT", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ expectedUpdatedAt, updateConfirmed: confirmed, status, internalNote: note }) }); const body = await response.json().catch(() => null); if (response.ok && typeof body?.redirectTo === "string" && body.redirectTo.startsWith("/admin/inquiries/")) location.assign(body.redirectTo); else setMessage(body?.error === "edit_conflict" ? "다른 관리자가 문의를 먼저 변경했습니다. 새로고침 후 다시 확인해 주세요." : body?.error === "invalid_transition" ? "현재 상태에서 선택한 상태로 변경할 수 없습니다. 페이지를 새로고침해 주세요." : body?.error === "validation" ? "입력 내용을 확인해 주세요." : "문의 처리 정보를 저장하지 못했습니다. 잠시 후 다시 시도해 주세요."); } catch { setMessage("문의 처리 정보를 저장하지 못했습니다. 잠시 후 다시 시도해 주세요."); } finally { setBusy(false); } } const changed = () => setConfirmed(false); return <form onSubmit={submit} aria-busy={busy} className="space-y-5"><div><label className="font-bold" htmlFor="status">처리 상태</label><select id="status" value={status} onChange={(e) => { setStatus(e.target.value as InquiryStatus); changed(); }} className="mt-2 block min-h-11 w-full rounded-control border p-2">{options.map((s) => <option key={s} value={s}>{inquiryStatusLabels[s]}</option>)}</select></div><div><label className="font-bold" htmlFor="internal-note">내부 메모</label><textarea id="internal-note" rows={8} maxLength={2000} value={note} onChange={(e) => { setNote(e.target.value); changed(); }} className="mt-2 block w-full rounded-control border p-2" /><p className="mt-2 text-small font-bold">내부 메모에도 주민등록번호, 계좌·카드번호, 건강·장애 정보와 입소자 개인정보를 기록하지 마세요.</p></div><label className="flex gap-3"><input type="checkbox" checked={confirmed} onChange={(e) => setConfirmed(e.target.checked)} /><span>선택한 처리 상태와 내부 메모를 확인했습니다.</span></label><button disabled={!confirmed || busy} className="min-h-11 rounded-control bg-primary px-5 py-2 font-bold text-primary-foreground disabled:opacity-50">저장</button><p role="alert">{message}</p></form>; }

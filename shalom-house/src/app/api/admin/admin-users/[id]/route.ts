import { authorizeCurrentAdmin } from "@/features/admin-auth/admin-authorization";import { isSameOriginRequest } from "@/features/admin-auth/admin-auth.service";import { updateAdminUser } from "@/features/admin-users/admin-user.admin-repository";import { validateUpdateAdminUserInput } from "@/features/admin-users/admin-user.validation";export const runtime="nodejs";const MAX=32*1024,json=(b:unknown,s:number)=>Response.json(b,{status:s,headers:{"Cache-Control":"no-store"}});export async function PUT(request:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;if(!/^[0-9a-f]{24}$/.test(id))return json({ok:false,error:"not_found"},404);if(!isSameOriginRequest(request))return json({ok:false,error:"forbidden"},403);const a=await authorizeCurrentAdmin("admin_users.manage");if(!a.ok)return json({ok:false,error:a.reason},a.reason==="unauthorized"?401:403);try{if(request.headers.get("content-type")?.split(";",1)[0].trim().toLowerCase()!=="application/json")return json({ok:false,error:"unsupported_media_type"},415);const l=request.headers.get("content-length");if(l&&Number(l)>MAX)return json({ok:false,error:"payload_too_large"},413);const text=await request.text();if(new TextEncoder().encode(text).byteLength>MAX)return json({ok:false,error:"payload_too_large"},413);let raw:unknown;try{raw=JSON.parse(text)}catch{return json({ok:false,error:"invalid_json"},400)}const v=validateUpdateAdminUserInput(raw);if(!v.ok)return json({ok:false,error:"validation",fieldErrors:v.fieldErrors},400);const r=await updateAdminUser({id,value:v.value,actor:a.admin});if(!r.ok)return json({ok:false,error:r.reason},r.reason==="not_found"?404:r.reason==="invalid_document"?503:409);return json({ok:true,redirectTo:`/admin/admin-users/${id}?updated=1`,revokedSessionCount:r.revokedSessionCount},200)}catch(error){console.error("관리자 계정 관리 실패",{targetAdminUserId:id,actorAdminUserId:a.admin.id,operation:"update",errorName:error instanceof Error?error.name:"UnknownError"});return json({ok:false,error:"unavailable"},503)}}

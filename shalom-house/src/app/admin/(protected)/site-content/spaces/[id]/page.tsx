import Link from "next/link"; import { notFound, redirect } from "next/navigation"; import { AdminAuditHistory } from "@/components/admin/admin-audit-history"; import { getCurrentAdmin } from "@/features/admin-auth/admin-auth.service"; import { hasAdminPermission } from "@/features/admin-auth/admin-authorization"; import { getAdminFacilitySpace } from "@/features/facility-spaces/facility-space.admin-repository"; import { getFacilitySpacePublicationStatusLabel } from "@/features/facility-spaces/facility-space.types";
export default async function Page({ params, searchParams }: { params: Promise<{ id: string }>; searchParams: Promise<{ saved?: string }> }) { const admin = await getCurrentAdmin(); if (!admin || !hasAdminPermission(admin, "site_content.manage")) redirect("/admin?forbidden=1"); const { id } = await params; const d = await getAdminFacilitySpace(id); if (!d) notFound(); const rows = [["공간명", d.title], ["공간 설명", d.description], ["공개 상태", getFacilitySpacePublicationStatusLabel(d.publicationStatus)], ["표시 순서", String(d.displayOrder)], ["생성일", d.createdAt], ["수정일", d.updatedAt], ["게시일", d.publishedAt ?? "없음"], ["보관일", d.archivedAt ?? "없음"]]; return <div className="space-y-8"><header><Link href="/admin/site-content/spaces" className="underline">← 생활공간 관리</Link><h1 className="mt-4 text-title font-bold">생활공간 상세</h1>{(await searchParams).saved === "1" ? <p role="status">저장했습니다.</p> : null}<Link className="mt-4 inline-block min-h-11 underline" href={`/admin/site-content/spaces/${id}/edit`}>편집</Link></header><dl className="grid gap-4 rounded-card border p-5 sm:grid-cols-2">{rows.map(([label, value]) => <div key={label}><dt className="font-bold text-muted-foreground">{label}</dt><dd className="whitespace-pre-wrap">{value}</dd></div>)}</dl><AdminAuditHistory items={d.auditHistory}/></div>; }

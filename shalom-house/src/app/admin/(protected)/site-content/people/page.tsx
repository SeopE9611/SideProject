import Link from "next/link"; import { redirect } from "next/navigation"; import { getCurrentAdmin } from "@/features/admin-auth/admin-auth.service"; import { hasAdminPermission } from "@/features/admin-auth/admin-authorization"; import { getStaffPublicationStatusLabel } from "@/features/staff/staff.types"; import { listAdminStaffProfiles } from "@/features/staff/staff.admin-repository";
export default async function Page(){const admin=await getCurrentAdmin();if(!admin||!hasAdminPermission(admin,"site_content.manage"))redirect("/admin?forbidden=1");const profiles=await listAdminStaffProfiles();return <div className="space-y-6"><header><Link href="/admin/site-content" className="underline">← 공식 콘텐츠 관리</Link><h1 className="mt-4 text-title font-bold">함께하는 사람들 관리</h1><div className="mt-4 flex gap-5"><Link className="min-h-11 font-bold underline" href="/admin/site-content/people/new">직원 소개 등록</Link><Link className="min-h-11 underline" href="/about/people">공개 페이지 보기</Link></div></header>{profiles.length?<div className="overflow-x-auto"><table className="w-full"><thead><tr>{["직책·역할","공개 이름","공개 상태","표시 순서","최근 수정","상세","편집"].map(x=><th className="p-3 text-left" key={x}>{x}</th>)}</tr></thead><tbody>{profiles.map(p=><tr className="border-t" key={p.id}><td className="p-3">{p.role}</td><td className="p-3">{p.publicName}</td><td className="p-3">{getStaffPublicationStatusLabel(p.publicationStatus)}</td><td className="p-3">{p.displayOrder}</td><td className="p-3"><time dateTime={p.updatedAt}>{p.updatedAt}</time></td><td><Link className="underline" href={`/admin/site-content/people/${p.id}`}>상세</Link></td><td><Link className="underline" href={`/admin/site-content/people/${p.id}/edit`}>편집</Link></td></tr>)}</tbody></table></div>:<p className="border-y py-6">등록된 직원 소개가 없습니다.</p>}</div>}

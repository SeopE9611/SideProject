import Link from"next/link";import{notFound,redirect}from"next/navigation";import{authorizeCurrentAdmin}from"@/features/admin-auth/admin-authorization";import{getAdminDonation}from"@/features/donations/donation.admin-repository";import{donationAuditActionLabels,donationAuditFieldLabels}from"@/features/donations/donation.audit";import{donationMethodLabels,donationPurposeLabels,donationReceiptStatusLabels,donationStatusLabels}from"@/features/donations/donation.types";export default async function Page({params}:{params:Promise<{id:string}>}){const a=await authorizeCurrentAdmin("donations.manage");if(!a.ok)redirect("/admin?forbidden=1");const d=await getAdminDonation((await params).id);if(!d)notFound();const date=(x:string|null)=>x?new Date(x).toLocaleString("ko-KR"):"—";return <div><h1 className="text-title font-bold">후원금 상세</h1>{d.status==="confirmed"&&<p className="mt-4">확정된 후원금의 금액·일자·후원자는 수정할 수 없습니다. 오류가 있으면 기존 기록을 무효 처리한 뒤 새 기록을 등록하세요.</p>}<dl className="mt-6 grid gap-4 sm:grid-cols-2"><div><dt>참조번호</dt><dd>{d.reference}</dd></div><div><dt>후원자 snapshot</dt><dd>{d.donorNameSnapshot} ({d.donorReferenceSnapshot??"익명"})</dd></div><div><dt>후원 일자</dt><dd>{d.donatedOn}</dd></div><div><dt>금액</dt><dd>{new Intl.NumberFormat("ko-KR").format(d.amountWon)}원</dd></div><div><dt>방식</dt><dd>{donationMethodLabels[d.method]}</dd></div><div><dt>목적</dt><dd>{donationPurposeLabels[d.purpose]} {d.purposeDescription}</dd></div><div><dt>영수증 처리</dt><dd>{donationReceiptStatusLabels[d.receiptStatus]} {d.receiptIssuedOn??""}</dd></div><div><dt>상태</dt><dd>{donationStatusLabels[d.status]}</dd></div><div><dt>무효 사유</dt><dd>{d.voidReason||"—"}</dd></div><div><dt>내부 메모</dt><dd>{d.internalNote||"—"}</dd></div><div><dt>생성일 / 수정일</dt><dd>{date(d.createdAt)} / {date(d.updatedAt)}</dd></div><div><dt>확정일 / 무효일</dt><dd>{date(d.confirmedAt)} / {date(d.voidedAt)}</dd></div></dl><Link className="mt-5 inline-block"href={`/admin/donations/${d.id}/edit`}>편집</Link><h2 className="mt-10 text-heading font-bold">감사 이력</h2>{d.audit.map((e:any,i:number)=><p key={i}className="mt-3 border-t pt-3">{donationAuditActionLabels[e.action]} · {e.displayName} · {date(e.occurredAt)} · {e.changedFields.map((x:any)=>donationAuditFieldLabels[x]).join(", ")} · {e.fromStatus?donationStatusLabels[e.fromStatus]:"신규"} → {donationStatusLabels[e.toStatus]}</p>)}</div>}

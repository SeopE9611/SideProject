import{redirect}from"next/navigation";import{authorizeCurrentAdmin}from"@/features/admin-auth/admin-authorization";import{AdminUserCreateForm}from"@/components/admin/admin-user-create-form";export default async function Page(){const a=await authorizeCurrentAdmin("admin_users.manage");if(!a.ok)redirect("/admin?forbidden=1");return <div><h1 className="text-title font-bold">관리자 계정 추가</h1><div className="mt-4"><p>초기 비밀번호는 안전한 별도 경로로 당사자에게 전달하세요.</p><p>비밀번호를 이메일·메신저 공개 채널이나 감사 메모에 기록하지 마세요.</p></div><AdminUserCreateForm/></div>}

import { hasAdminPermission } from "@/features/admin-auth/admin-authorization";
import { getCurrentAdmin } from "@/features/admin-auth/admin-auth.service";
import Link from "next/link";
import { listAdminPrograms, normalizeAdminProgramPage, type AdminProgramListFilters } from "@/features/programs/program.admin-repository";
import { getProgramApprovalStatusLabel, getProgramPublicationStatusLabel, isProgramApprovalStatus, isProgramPublicationStatus } from "@/features/programs/program.types";
const date = new Intl.DateTimeFormat("ko-KR",{year:"numeric",month:"long",day:"numeric",timeZone:"UTC"});
type Params={publicationStatus?:string|string[];approvalStatus?:string|string[];page?:string|string[];created?:string|string[];updated?:string|string[];reviewRequested?:string|string[];approved?:string|string[];rejected?:string|string[];published?:string|string[];unpublished?:string|string[];archived?:string|string[]};
function href(page:number,f:AdminProgramListFilters){const q=new URLSearchParams();if(f.publicationStatus)q.set("publicationStatus",f.publicationStatus);if(f.approvalStatus)q.set("approvalStatus",f.approvalStatus);if(page>1)q.set("page",String(page));return q.size?`/admin/programs?${q}`:"/admin/programs";}
export default async function AdminProgramsPage({searchParams}:{searchParams:Promise<Params>}){const admin = await getCurrentAdmin();const canCreate=Boolean(admin&&hasAdminPermission(admin,"content.create"));const q=await searchParams;const publicationStatus=typeof q.publicationStatus==="string"&&isProgramPublicationStatus(q.publicationStatus)?q.publicationStatus:undefined;const approvalStatus=typeof q.approvalStatus==="string"&&isProgramApprovalStatus(q.approvalStatus)?q.approvalStatus:undefined;const filters={publicationStatus,approvalStatus};const result=await listAdminPrograms({page:normalizeAdminProgramPage(typeof q.page==="string"?q.page:undefined),filters});const messages:[[keyof Params,string]]|[keyof Params,string][]=[["created","프로그램 초안을 저장했습니다."],["updated","프로그램을 수정했습니다."],["reviewRequested","검토를 요청했습니다."],["approved","검토를 승인했습니다."],["rejected","프로그램을 반려했습니다."],["published","프로그램을 게시했습니다."],["unpublished","게시를 중단했습니다."],["archived","프로그램을 보관했습니다."]];const message=messages.find(([key])=>q[key]==="1")?.[1];return <div className="space-y-8"><header><h1 className="text-title font-bold">프로그램 관리</h1><p className="mt-3 text-body text-muted-foreground">프로그램의 작성, 검토, 승인과 공개 상태를 관리합니다.</p>{canCreate?(<Link href="/admin/programs/new" className="mt-4 inline-flex min-h-11 items-center rounded-control bg-primary px-4 py-2 font-semibold text-primary-foreground">프로그램 작성</Link>):null}</header>{message?<p role="status" className="border border-border-strong bg-surface p-4 font-semibold">{message}</p>:null}<section aria-labelledby="program-filter"><h2 id="program-filter" className="text-heading font-bold">상태 필터</h2><form className="mt-4 grid gap-4 sm:grid-cols-2" action="/admin/programs"><label className="grid gap-2 font-semibold">게시 상태<select name="publicationStatus" defaultValue={publicationStatus??""} className="min-h-11 border border-border-strong bg-background px-3"><option value="">전체</option><option value="draft">작성 중</option><option value="review">검토 중</option><option value="published">게시</option><option value="archived">보관</option></select></label><label className="grid gap-2 font-semibold">승인 상태<select name="approvalStatus" defaultValue={approvalStatus??""} className="min-h-11 border border-border-strong bg-background px-3"><option value="">전체</option><option value="pending">승인 대기</option><option value="approved">승인 완료</option><option value="rejected">반려</option></select></label><button className="min-h-11 bg-primary px-4 font-semibold text-primary-foreground sm:col-span-2">필터 적용</button></form></section><section aria-labelledby="program-list"><h2 id="program-list" className="sr-only">프로그램 목록</h2>{result.items.length?<ul className="border-t border-foreground">{result.items.map(item=><li key={item.id} className="grid gap-3 border-b border-border py-5 xl:grid-cols-[2fr_1fr_0.6fr_0.8fr_0.8fr_1fr_0.7fr]"><div><Link href={`/admin/programs/${item.id}`} className="text-heading font-bold underline-offset-4 hover:underline">{item.title}</Link><p className="text-small text-muted-foreground">{item.category}</p></div><p><strong className="xl:sr-only">운영 상태 </strong>{item.operationStatusLabel??"미입력"}</p><p><strong className="xl:sr-only">정렬 </strong>{item.sortOrder}</p><p>{getProgramPublicationStatusLabel(item.publicationStatus)}</p><p>{getProgramApprovalStatusLabel(item.approvalStatus)}</p><time dateTime={item.updatedAt}>{date.format(new Date(item.updatedAt))}</time><p>{item.isPubliclyVisible?"공개 중":"비공개"}</p></li>)}</ul>:<p className="border-y border-border py-6">등록된 프로그램이 없습니다.</p>}</section>{result.totalPages>1?<nav aria-label="프로그램 목록 페이지 이동" className="flex justify-center gap-4">{result.page>1?<Link href={href(result.page-1,filters)}>이전</Link>:<span>이전</span>}<span>{result.page} / {result.totalPages}</span>{result.page<result.totalPages?<Link href={href(result.page+1,filters)}>다음</Link>:<span>다음</span>}</nav>:null}</div>}

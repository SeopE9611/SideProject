# 디자인 시스템

## 브랜드 방향

- 공공기관의 명확성과 복지기관의 존중·따뜻함을 함께 전달한다.
- 장식보다 기관 신뢰와 사용자 과업, 실제 정보를 우선하고 감정적인 후원 유도를 하지 않는다.
- 일반 감성 문구 대신 승인된 시설 정보와 실제 생활·관계를 구체적으로 설명한다.
- 기존 UI는 보존 대상이 아니라 검토 대상이며, 같은 대형 히어로를 모든 페이지에 반복하지 않는다.

## 토큰 단일 출처와 상태

디자인 토큰의 실제 값은 `src/app/globals.css`를 단일 출처로 사용한다. 이 문서는 토큰의 의미, 허용 용도와 폐기 방향을 정의한다. Markdown과 CSS 값이 충돌하면 실제 값은 `globals.css`에서 확인하며, 토큰 값을 바꾸는 UI 작업은 이 문서와 `globals.css`를 함께 갱신한다.

현재 토큰값은 구현 현황이며 목표 디자인의 합격 근거가 아니다. 아래 시각 언어와 페이지별 검수 기준으로 조합을 판단한다.

## 대표 화면의 시각 언어

- 헤더는 흰색 표면에 30~~34px 기관명과 데스크톱 18~~20px의 4개 상위 메뉴를 배치한다. 남색 기관명과 청록색 현재 위치 표시로 위계를 구분한다. 상단 보조 링크는 12px로 낮춰 주요 메뉴와 경쟁하지 않게 한다.
- 상위 메뉴명은 해당 페이지로 이동하고 옆 버튼은 하위 메뉴를 연다. 클릭·키보드로 열고 닫으며 Escape로 닫으면 버튼으로 포커스를 돌린다. hover 진입과 클릭 토글을 겹쳐 실행하지 않는다.
- 페이지 내 세부 메뉴는 16px와 청록색 현재 위치 밑줄을 사용한다. 좁은 화면에서는 다음 줄로 이어져 현재 페이지가 가로 스크롤 밖에 숨지 않게 한다.
- 페이지의 가로 정렬은 `max-w-site`와 공통 좌우 간격을 사용한다. 본문·목록은 흰색, 기관 소개나 보조 문의 영역은 옅은 중립 표면을 사용한다. 배경색을 매 섹션마다 번갈아 쓰지 않는다.
- 대표 제목은 홈 44~~64px, 일반 페이지 32~~40px를 출발점으로 사용한다. 홈 섹션 제목은 28~~32px, 일반 섹션은 24px, 목록 제목은 18~~20px, 본문은 16~~18px로 구분한다. 홈의 최신 소식 한 건은 20~~24px 제목과 요약으로 나머지 목록보다 강조한다. 이 값은 프로젝트의 설계 기준이며 접근성 표준 수치가 아니다.
- 페이지 제목 앞에 경로와 같은 단어의 작은 라벨을 반복하지 않는다. 설명은 방문자가 할 수 있는 일을 짧게 안내하며 모든 제목에 설명을 붙일 필요는 없다.
- 일반 섹션은 32~~48px, 제목과 콘텐츠는 16~~24px, 목록 항목은 16~20px의 세로 간격을 출발점으로 삼는다. 높이는 콘텐츠에 따라 늘어나며 빈 칸을 맞추기 위한 고정 높이는 사용하지 않는다.
- 목록은 제목·분류·날짜를 정렬하고 가는 구분선 하나로 항목을 나눈다. 사진 기록은 일정한 이미지 비율과 날짜·제목을 함께 사용한다. 참여 안내는 유형과 다음 행동을 연결한다.
- 컨트롤에는 작은 모서리, 명확한 경계와 포커스를 사용한다. 본문 영역에는 그림자를 사용하지 않으며 열린 메뉴처럼 떠 있는 표면에만 그림자를 사용한다.
- 한국어는 프로젝트에 포함한 Pretendard Variable을 사용한다. 기관명과 대표 제목은 700~~800, 본문은 400~~500, 메뉴는 700으로 구분한다. 외부 글꼴 서비스에 의존하지 않으며 글꼴과 함께 배포 라이선스를 보관한다. 새 패키지 설치는 필요하지 않다.
- 대표색은 남색, 행동 강조는 청록색으로 구분한다. 흰색 본문, 옅은 청회색 기록 영역, 웜 페이퍼색 방문·참여 보조 영역을 각 역할에 맞게 사용한다. 어두운 남색 참여 영역과 푸터는 서로 떨어뜨려 사용한다. 홈의 정보 순서와 사진 없는 구성은 `page-patterns.md`를 따른다.

### 참고 요소

| 참고                                           | 실제 화면에서 관찰한 구성                                        | 샬롬의 집에 적용할 원칙                                                                                                                   |
| ---------------------------------------------- | ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| [서울대학교](https://www.snu.ac.kr/index.html) | 기관 고유 영상과 큰 시각 자료, 방문 대상별 탐색, 사진 중심 소식  | 승인된 활동 기록을 크게 보여 주되 방문 목적별 안내를 별도 구획으로 제공한다. 전면 자동재생 영상은 가져오지 않는다.                        |
| [사랑의열매](https://chest.or.kr/base.do)      | 굵은 제목, 선명한 대표색 버튼, 캠페인과 기부 영역의 분리         | 제목·본문의 굵기 차이와 대표색의 역할을 명확히 하고 참여 안내를 소식 목록과 구별한다. 모금액 입력이나 과도한 캠페인 수는 도입하지 않는다. |
| [한국수자원공사](https://www.kwater.or.kr/)    | 절제된 흰색 기관 헤더, 강한 대표색 시각 영역, 주요 업무 바로가기 | 헤더·대표 영역·업무 탐색의 크기와 대비를 달리한다. 반복 링크에는 실제 목적을 나타내는 선형 아이콘을 제한적으로 쓴다.                      |
| [서울특별시](https://www.seoul.go.kr/)         | 긴급 공지, 통합검색과 주요 서비스의 높은 우선순위                | 긴급하거나 중요한 실제 안내가 있을 때만 일반 소식보다 먼저 보여 준다. 다중 배너와 과밀한 자동 슬라이드는 가져오지 않는다.                 |
| [밀알복지재단](https://www.miral.org/main.do)  | 굵은 대표 제목, 사진 중심 이야기, 연차보고와 투명성 정보         | 승인된 활동 기록과 신뢰 자료의 위계를 강화한다. 개인의 얼굴과 사연을 감정적 참여 유도에 사용하지 않는다.                                  |

참고 사이트 전체를 목표 화면으로 간주하지 않는다. 자동 슬라이드·영상·팝업이나 정부 전용 식별자를 도입하지 않고 기관명·문구·이미지·코드를 복제하지 않는다.

컴포넌트에 임의 HEX, RGB, HSL, OKLCH 값을 직접 추가하지 않는다. Tailwind arbitrary value는 기존 토큰으로 표현할 수 없는 명확한 이유가 있고 검토 범위가 기록된 경우에만 사용한다.

- **유지**: 새 UI에서도 계속 사용하는 의미 기반 토큰이다.
- **제한 사용**: 지정된 상태나 컴포넌트에서만 사용한다.
- **점진적 폐기 검토**: 기존 랜딩페이지와 강하게 결합된 홈·히어로 토큰이다.

### 현재 토큰 분류표

여러 토큰을 묶은 행의 현재 값은 토큰 표기 순서와 같다. `--shadow-card`의 실제 값은 두 줄 선언이므로 CSS에서 확인한다.

| 분류                     | 토큰                                                                                                                                                    | 현재 값                                                                                  | 허용 용도                                      | 상태             |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ---------------------------------------------- | ---------------- |
| 기본 배경과 표면         | `--color-background`, `--color-surface`, `--color-surface-subtle`                                                                                       | `#ffffff`, `#ffffff`, `#f2f5f7`                                                          | 페이지 배경, 기본·보조 표면                    | 유지             |
| 웜 보조 표면             | `--color-paper`, `--color-paper-strong`                                                                                                                 | `#f6f1e8`, `#e7dac6`                                                                     | 방문·참여 정보와 해당 표면의 경계              | 제한 사용        |
| 텍스트                   | `--color-foreground`, `--color-muted-foreground`                                                                                                        | `#1b2c3d`, `#566371`                                                                     | 본문과 보조 정보                               | 유지             |
| 경계                     | `--color-border`, `--color-border-strong`                                                                                                               | `#dce3e8`, `#72818d`                                                                     | 구분선과 강한 경계                             | 유지             |
| 주요색                   | `--color-primary`, `--color-primary-hover`, `--color-primary-soft`, `--color-primary-foreground`                                                        | `#17384d`, `#102b3c`, `#eaf1f5`, `#ffffff`                                               | 브랜드, 주요 행동과 그 상태                    | 유지             |
| 강조색                   | `--color-accent`, `--color-accent-hover`, `--color-accent-soft`                                                                                         | `#087e83`, `#06656a`, `#e7f3f2`                                                          | 제한적 강조와 상태                             | 제한 사용        |
| 상태색                   | `--color-success`, `--color-success-soft`, `--color-warning`, `--color-warning-soft`, `--color-danger`, `--color-danger-soft`                           | `#2f6b4f`, `#eaf4ee`, `#7a520f`, `#fff4d9`, `#a23b3b`, `#fbecec`                         | 성공·경고·오류의 텍스트와 표면                 | 제한 사용        |
| 어두운 표면              | `--color-on-dark`, `--color-sun-soft`                                                                                                                   | `#ffffff`, `#ead39b`                                                                     | 의미 기반 어두운 표면의 전경과 제한적 강조     | 제한 사용        |
| 홈·히어로 전용 기존 토큰 | `--color-hero-forest`, `--color-hero-clay`, `--color-hero-night`, `--color-hero-on-dark`, `--color-hero-muted`, `--color-hero-mist`, `--color-hero-sun` | `#1f493f`, `#85452f`, `#19363a`, `#ffffff`, `#edf3ef`, `#d9e5df`, `#ead39b`              | 현재 `PageHero` 등 기존 호출부 호환            | 점진적 폐기 검토 |
| 홈·히어로 전용 기존 토큰 | `--color-home-cream`, `--color-home-ink`, `--color-home-sun`, `--color-home-coral`, `--color-home-sky`, `--color-home-lilac`                            | `#f1ece2`, `#183b3b`, `#e6d7aa`, `#d9a08c`, `#b9d2c8`, `#c9c5cf`                         | 현재 `HomeHero`와 공개 페이지 기존 호출부 호환 | 점진적 폐기 검토 |
| 포커스                   | `--color-focus-ring`                                                                                                                                    | `#087e83`                                                                                | `focus-visible` 표시                           | 유지             |
| 타이포그래피             | `--text-hero`, `--text-hero-lg`, `--text-display`, `--text-display-lg`                                                                                  | `2.625rem`, `4.5rem`, `2.5rem`, `3.5rem`                                                 | 대표·디스플레이 제목                           | 제한 사용        |
| 타이포그래피             | `--text-title`, `--text-heading`, `--text-body`, `--text-small`                                                                                         | `2rem`, `1.5rem`, `1.0625rem`, `1rem`                                                    | 제목, 본문, 보조 정보                          | 유지             |
| 컨테이너                 | `--container-site`, `--container-content`                                                                                                               | `84rem`, `48rem`                                                                         | 전체 사이트 폭과 읽기 폭                       | 유지             |
| 간격                     | `--spacing-page`, `--spacing-page-wide`, `--spacing-section`, `--spacing-section-wide`                                                                  | `1.25rem`, `2.5rem`, `5rem`, `7rem`                                                      | 페이지 좌우와 섹션 간격                        | 유지             |
| 모서리                   | `--radius-control`, `--radius-card`, `--radius-panel`                                                                                                   | `0.5rem`, `0.875rem`, `1.25rem`                                                          | 컨트롤, 기능 카드, 강조 패널                   | 제한 사용        |
| 그림자                   | `--shadow-card`, `--shadow-nav`, `--shadow-elevated`                                                                                                    | CSS의 다중 그림자, `0 8px 24px rgb(32 47 53 / 0.07)`, `0 22px 54px rgb(24 59 59 / 0.14)` | 필요한 카드·내비게이션·부유 표면               | 제한 사용        |
| 모션                     | `--ease-standard`, `--motion-duration-fast`, `--motion-duration-standard`                                                                               | `cubic-bezier(0.2, 0, 0, 1)`, `150ms`, `240ms`                                           | 상태 전환과 짧은 피드백                        | 유지             |
| 모션                     | `--motion-duration-hero`                                                                                                                                | `520ms`                                                                                  | 기존 히어로 모션 호환                          | 점진적 폐기 검토 |

`home-*`, `hero-*` 토큰과 `--motion-duration-hero`는 기존 화면과의 호환을 위해 유지한다. 새 대표 화면은 의미 기반 표면·텍스트 토큰을 사용한다. 삭제는 실제 호출부를 검색하여 제거한 뒤 별도 작업으로 진행한다.

### 배경과 표면 사용 규칙

- `--color-background`는 기존 코드 호환을 위해 유지하며 전역 캔버스 또는 제한된 중립 배경에 사용한다. 모든 섹션의 기본 배경으로 반복하지 않는다.
- 공지 목록, 본문, 상세 콘텐츠와 기관 정보 영역은 기본 콘텐츠 표면인 `--color-surface`를 우선한다.
- 웜 페이퍼 배경은 방문·문의와 참여 보조 정보처럼 지정된 일부 섹션에만 사용하며 페이지 전체를 웜 아이보리로 채우지 않는다. 웜 표면의 경계는 `--color-paper-strong`을 사용한다.
- 어두운 배경은 핵심 CTA 또는 중요 안내 한 곳에 제한적으로 사용하고 페이지마다 대형 어두운 패널을 반복하지 않는다. 홈과 소식 페이지에는 어두운 인용문 패널을 사용하지 않는다.

## 타이포그래피

대표 화면의 크기와 간격은 위 시각 언어를 따른다. 기존 display·hero 토큰의 크기를 새 화면에 자동 적용하지 않는다. 역할은 다음처럼 구분한다.

`--font-sans`는 `next/font/local`의 Pretendard Variable을 우선하고 `Apple SD Gothic Neo`, `Malgun Gothic`, `sans-serif`를 대체 글꼴로 사용한다. 원본과 라이선스는 `src/app/fonts/`에 두며 `font-display: swap`을 사용한다. [공식 배포](https://github.com/orioncactus/pretendard)는 SIL Open Font License를 포함한다. 글꼴 로딩 전후 줄바꿈과 큰 제목의 자간은 실제 화면에서 확인한다.

| 역할                  | 사용 기준                                                                 |
| --------------------- | ------------------------------------------------------------------------- |
| 홈 대표 제목          | 사이트 입구의 단일 `h1`; 대형 표현은 한 화면에서 반복하지 않음            |
| 일반 페이지 제목      | 페이지 목적을 설명하는 단일 `h1`; 홈보다 낮은 위계                        |
| 목록·검색 페이지 제목 | 검색·분류 도구와 결과가 우선 보이도록 절제한 `h1`                         |
| 섹션 제목             | 주요 모듈을 구분하는 `h2`; 대표 화면에서는 `text-heading`을 기본으로 사용 |
| 카드·목록 제목        | 항목 이름을 나타내는 `h3` 또는 문맥상 제목; 대표 목록에서는 18~20px 사용  |
| 본문                  | 설명과 상세 콘텐츠; `text-body`와 읽기 폭 사용                            |
| 보조 정보             | 부연 설명·도움말; 본문보다 낮되 읽을 수 있는 대비 유지                    |
| 메타데이터            | 날짜·분류·파일 정보; `text-small`을 기본으로 관계를 명확히 함             |
| 버튼과 링크           | 동작 또는 목적이 드러나는 짧은 문구; 크기보다 의미를 우선함               |

글자 크기만으로 제목 단계를 정하지 않는다. `h1`부터 `h3`의 문서 구조와 시각 크기는 독립적으로 관리한다. 본문은 `--container-content` 등으로 한 줄이 지나치게 길어지지 않게 한다. 한국어에는 `word-break: keep-all`과 `overflow-wrap: break-word`를 함께 사용하며 URL, 파일명과 공백 없는 문자열도 레이아웃 밖으로 넘치지 않아야 한다.

독립된 안내·연락 링크에는 `institution-link` 유틸리티를 사용한다. 최소 높이 44px, 주요색, hover 밑줄과 focus 표시를 공통으로 제공한다. 본문 문장 속 링크나 여러 행의 목록 제목에는 문맥에 맞는 별도 스타일을 적용한다.

## 그리드와 반응형

- **모바일**: 단일 열이 기본이며 정보 순서와 DOM 순서를 일치시킨다. 가로 카드 나열을 강제하지 않고 기능 링크의 최소 대상 크기를 확보하며 이미지·텍스트 좌우 분할은 세로 구조로 바꾼다.
- **태블릿**: 콘텐츠 목적에 따라 1열 또는 2열을 선택한다. 탐색 카드와 소식 목록의 정보 밀도를 유지하고 데스크톱을 단순 축소하지 않는다.
- **데스크톱**: 12열 안에서 페이지 유형별 열 비율을 사용한다. 본문 읽기 폭을 제한하고 여백을 콘텐츠 부재의 대체 수단으로 쓰지 않는다.

실제 시각 검증 viewport는 `320px`, `390px`, `768px`, `1024px`, `1440px`다. 페이지 유형별 구조는 [`page-patterns.md`](./page-patterns.md), 접근성과 대상 크기는 [`accessibility.md`](./accessibility.md)를 따른다.

## 간격과 밀도

현재 spacing 토큰과 기존 spacing 시스템 안에서 페이지 시작, 일반 섹션, 기능 섹션, 목록 내부 간격을 구분한다. 모든 섹션에 같은 상하 간격을 반복하거나 빈 콘텐츠를 큰 여백으로 채우지 않는다. 정보량이 섹션 높이를 자연스럽게 결정해야 하며 고정 높이로 본문을 자르지 않는다. `min-height`는 목적이 명확한 히어로·상태 영역에만 사용한다.

## 표면과 카드

### 기본 콘텐츠 영역

본문, 공지 목록, 소개 문장, 문서 목록과 상세 콘텐츠에 사용한다. 흰색 또는 중립 배경에서 그림자 대신 제목, 여백과 구분선으로 계층을 만든다.

### 기능 카드

실제로 이동 가능한 독립 기능, 프로그램·지원 영역, 연락처·방문 정보와 활동 콘텐츠에만 명확한 테두리, 작은 모서리와 일관된 내부 간격을 사용한다. 짧은 설명 하나, 같은 내용의 반복 장식 또는 빈 공간을 채우기 위한 카드는 금지한다.

### 강조 패널

중요 공지, 참여 CTA, 긴급하지 않은 핵심 안내와 대표 문의 경로에 페이지당 최대 1개를 기본으로 사용한다. 장식용으로 반복하지 않는다.

## 버튼과 링크

외형이 아니라 실제 동작 의미로 선택한다. 링크는 경로·문서로 이동하고 버튼은 현재 맥락의 동작을 실행한다.

| 패턴             | 목적과 텍스트                                      | 상태와 표시                                                                                           |
| ---------------- | -------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| Primary action   | 페이지의 단일 우선 행동; 동사와 대상을 명확히 작성 | hover·active에 의미 토큰을 쓰고 `focus-visible`을 보존한다. 실행 불가 시 비활성 이유를 함께 제공한다. |
| Secondary action | 우선순위가 낮은 보조 행동                          | Primary와 시각 위계를 구분하되 동일한 focus·active 기준을 적용한다.                                   |
| Text link        | 본문 안의 관련 페이지 이동                         | hover에서도 밑줄 등 링크 단서를 유지하고 현재 페이지면 `aria-current`를 검토한다.                     |
| Navigation link  | 전역·지역 탐색                                     | 목적지 이름을 쓰고 hover, focus-visible, active/현재 위치를 색상 외에도 구분한다.                     |
| Download link    | 파일 다운로드                                      | 문서명과 형식·크기를 알리고 아이콘은 장식이면 `aria-hidden`; 다운로드 불가는 이유를 표시한다.         |
| External link    | 외부 사이트 또는 새 창 이동                        | 목적지를 쓰고 새 창이면 사전 안내한다. 화살표 아이콘은 중복 낭독되지 않게 처리한다.                   |
| Telephone link   | 승인된 번호로 전화 연결                            | 화면에 읽을 수 있는 번호·용도를 제공하고 `tel:` 동작을 예측 가능하게 한다.                            |

모든 패턴은 hover만으로 정보를 전달하지 않고 `focus-visible`과 active 피드백을 제공한다. 비활성 링크처럼 보이는 가짜 링크를 만들지 않으며 disabled 버튼은 오류 원인을 숨기는 수단으로 쓰지 않는다. 새 창·다운로드 표시는 텍스트로 사전 안내하고 아이콘과 화살표가 같은 이름을 중복 낭독하지 않도록 장식이면 `aria-hidden="true"`를 사용한다.

## 이미지 패턴

| 유형                  | 권장 비율                           | 표시와 크롭 검토                                                               |
| --------------------- | ----------------------------------- | ------------------------------------------------------------------------------ |
| 홈 대표 이미지        | `3:2` 또는 `4:3`                    | 영역을 채울 때 `object-fit: cover`; 핵심 맥락이 잘리지 않는지 전 viewport 확인 |
| 활동 카드 이미지      | `4:3`                               | 같은 목록은 비율을 통일하고 활동 도구·공간의 초점 보존                         |
| 생활 기록 이미지      | `3:2` 또는 원본 맥락에 맞는 비율    | 기록 의미가 크롭으로 바뀌지 않게 하고 필요하면 `contain` 검토                  |
| 시설 공간 이미지      | `3:2` 또는 `4:3`                    | 공간 구조와 접근 정보가 잘리지 않게 크롭 위치 확인                             |
| 목록 썸네일           | 한 목록에서 동일한 `4:3` 또는 `3:2` | `cover` 기본, 이미지 없는 항목과 정렬·대체 상태 검증                           |
| 지도 또는 위치 이미지 | 제공 원본 비율                      | 표식·경로가 잘리면 `contain`; 확대·외부 지도 경로와 실제 위치 여부를 명시      |

최종은 공개 승인된 실제 사진을 우선한다. 개발 placeholder는 중앙 fixture 정책을 따르며 실제 사진처럼 설명하지 않는다. 실제 이미지가 없을 때 인용문 패널이나 실제 사진처럼 보이는 추상 그래픽으로 대체하지 않고 데이터 상태와 대체 텍스트에서 fixture placeholder임을 구분한다. 이미지 정보·대체 텍스트 기준은 [`accessibility.md`](./accessibility.md), 상태·승인은 [`content-governance.md`](./content-governance.md)가 소유한다.

### 공식 이미지가 없는 상태

- 실제 사진처럼 보이는 가짜 시설 일러스트나 집, 나무, 가구와 인물을 단순 도형으로 묘사한 대표 placeholder를 만들지 않는다.
- 개발용 이미지 검증에서는 실제 시설의 외관·공간·활동을 묘사하지 않는 중립적인 상태 영역을 사용할 수 있다.
- 공개 홈에 공식 이미지가 없으면 미디어 영역을 생략하고 텍스트와 업무 링크에 맞게 높이를 줄인다. 대형 사진 준비 중 패널을 필수 요소로 두지 않는다.
- 게시된 활동사진은 해당 활동의 기록으로 표시한다. 별도 승인 없이 활동사진을 시설 대표 사진으로 설명하지 않는다. 공개 조건을 재확인하는 기존 미디어 경로를 사용하고 이미지 최적화 캐시에 별도 사본을 만들지 않는다.
- placeholder에서는 이미지 영역의 비율, 텍스트 정렬과 반응형만 검증하고 승인 사진을 확보한 뒤 `official` 이미지로 교체한다.

## 공통 컴포넌트와 모션

기관 헤더·내비게이션, 브레드크럼과 소형 페이지 헤더, 홈 인트로, 공지 목록·활동 카드, 지원 영역, 문서 목록·절차, 연락·방문 패널과 푸터를 역할과 과업 기준으로 공유한다. 기존 `PageHero`를 모든 페이지의 필수 패턴으로 정의하지 않으며 `HomeHero`도 홈에 반복 적용할 일반 패턴이 아니다.

hover는 상호작용 요소에만 사용한다. 이미지 hover는 1.5~2% 이내 확대와 짧은 제목 밑줄을 출발점으로 하고 키보드 focus에서도 목적지를 구분한다. 배경 영상과 패럴랙스를 사용하지 않으며 모션을 제거해도 정보와 기능이 유지되어야 한다.

활동 기록 슬라이드는 공개 승인 항목이 3건 이상이고 각 항목의 제목·날짜·대체 텍스트·상세 경로가 있을 때만 검토한다. 기본 자동재생은 사용하지 않고 이전·다음, 현재 위치와 정지 상태를 텍스트로 제공한다. 한 건이면 슬라이드 조작부를 제거하고 단일 대표 기록으로 표시하며 `prefers-reduced-motion`에서는 즉시 전환한다.

## 관리자 화면

관리자 화면은 공개 사이트와 같은 색상 토큰을 사용하되 장식보다 업무 위치와 상태를 우선한다. 도깨비테니스 관리자 화면의 `페이지 셸 → 제목과 설명 → 업무 단위 섹션 → 입력과 오류 → 작업 버튼` 구조를 참고하며 샬롬의 집 권한·승인 절차에 맞게 단순화한다.

- 상단에는 서비스명, 현재 관리자와 공개 홈페이지 이동을 제공한다.
- 메뉴는 개요, 홈페이지 콘텐츠, 운영 업무로 구분하며 현재 메뉴를 색상과 왼쪽 선으로 함께 표시한다.
- 입력 컨트롤은 최소 48px 높이, 본문 16px 이상과 명확한 테두리를 유지한다.
- 필수 항목은 라벨에서 `*`로 표시하고 `required`, `maxLength`, `pattern`, 숫자 범위 등 브라우저 검증을 서버 검증과 함께 사용한다.
- 제출 오류는 필드 바로 아래에 표시하고 `aria-invalid`, `aria-describedby`로 연결한다. 여러 오류가 있으면 항목 수를 알리고 첫 오류로 이동할 수 있어야 한다.
- 저장, 취소와 상태 전환 버튼은 같은 작업 묶음으로 배치한다. 저장 중에는 중복 제출을 막고 현재 처리 상태를 알린다.
- 목록 표는 헤더와 행 경계를 분명히 하며 좁은 화면에서는 정보가 잘리거나 겹치지 않게 카드형 또는 가로 스크롤을 제공한다.
- 관리자 화면도 특정 1440px에 고정하지 않고 390px, 태블릿, 1024~1280px 노트북과 넓은 데스크톱에서 검수한다.

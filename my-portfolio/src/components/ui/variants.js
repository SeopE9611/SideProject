export const buttonVariants = {
  primary: 'btn-token btn-primary',
  outline: 'btn-token btn-outline',
  ghost: 'btn-token btn-ghost',
};

export const badgeVariant = 'badge-token px-3 py-1';

export const cardVariant = 'card-surface p-6';

export const iconBadgeVariant = 'p-3 rounded-full bg-accent text-primary';

export const linkVariant = 'link-accent inline-flex items-center gap-1';

export const overlayVariant = 'fixed inset-0 z-50 flex items-center justify-center bg-overlay';

export const modalPanelVariant = 'bg-card text-foreground border border-token p-6 rounded shadow-lg';
